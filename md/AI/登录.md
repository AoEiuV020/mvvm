
1. 以登录页为例，编写无关ui框架的文档，严格遵守.github/instructions/mvvm.instructions.md约束，
1. 核心功能假定已经有独立的sdk模块已实现无关ui的所有内容，编辑.github/instructions/project.instructions.md添加关于sdk的说明，添加文件专门介绍sdk暴露的api，

1. 就一个文件讲不完一个sdk的所有api吧，应该在md目录找个位置分级介绍sdk的用法，
1. 通用文档中定义接口合适吗？确实需要的话也不要用js/ts代码，尽可能伪代码或者flutter，这些是否有有必要写进.github/instructions/project.instructions.md

1. 你这伪代码感觉可读性还不如真代码， 考虑接口本身也没几行代码，就用真代码吧，尽量简单，代码比伪代码简单的话以flutter代码为例，

1. 不要有“取决于业务”这种暧昧的文字， 规划应当严格，不向下兼容，避免AI自由发挥，

1. 整体梳理一下所有文档看有没有冲突， 
1. 然后把用户信息username改成区号+手机号，

1. 认证相关完善， 实际项目是分为http登录获取id/token，没有完整的用户信息，登录后进入数据同步页获取用户信息/好友/同事/群组列表, 然后进入主页后建立长连接用id token进行tcp登录，这种情况sdk应该怎么设计， 考虑好再改文档，

1. md/规划/SDK/认证模块.md:35 登录api有必要返回成功boolean吗？难道不是返回了就是成功？

1. 我看你长连接登录不需要参数， 意思是sdk维护这些吗？是否需要说明约束好， 

1. 还是不够， 登录和注册应该获得loginToken+accessToken，而loginToken是用于下次启动时自动登录使用的， 也就是还得有个自动登录功能，通过loginToken刷新accessToken，这时候外部还是得保存数据，调整相关文档，

1. 我希望app层简单，sdk维护的话app得知道现在启动时该去登录页还是去主页进行自动登录，

1. md/规划/通用/登录注册 规划是不是应该加一个启动页，负责判断状态跳转登录或者主页？

1. 检查当前登录设计是否符合mvvm，主要是这种简单的表单页面是否需要model？

1. 像是表单校验之类的我听说应该放在model层，你觉得如何？当前文档是不是露了这块，
